# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual envs
.venv/
venv/
env/
ENV/

# Env files (never commit real secrets)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Testing / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/
coverage.xml
*.cover

# Airflow local state
airflow/logs/
airflow/airflow.db
airflow/airflow-webserver.pid
airflow/standalone_admin_password.txt

# Spark
spark-warehouse/
metastore_db/
derby.log
*.crc

# Great Expectations local artifacts
expectations/uncommitted/

# Data outputs (raw data is generated by scripts/generate_data.py)
# Keep the folder but ignore generated files
data/raw/*.csv
data/raw/*.jsonl
data/raw/*.json
!data/raw/.gitkeep

# MinIO local volume (if mounted)
minio-data/

# Postgres local volume
postgres-data/

# Internal planning / analysis / execution logs — kept locally, not shared
analisis_repo_profesor.md
comandos_a_ejecutar_pwd.txt
plan_definitivo.md

# Terraform local state and cached providers.
# The lock file (.terraform.lock.hcl) IS committed on purpose.
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
terraform/tfplan
terraform/crash.log
