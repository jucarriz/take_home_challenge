# Runtime Python deps for the Airflow image.
# These are installed on top of the apache/airflow:2.9.3-python3.11 base
# (which already brings SQLAlchemy, psycopg2, pandas, providers, ...).

pyspark==3.5.1
great-expectations==0.18.19
minio==7.2.7
httpx==0.27.2
psycopg2-binary==2.9.9
# Kafka producer for the streaming DAG.
kafka-python==2.0.2
# Test deps (kept in this file so pytest runs inside the airflow image
# without needing a second Python environment).
pytest==8.3.3
fastapi==0.115.0
